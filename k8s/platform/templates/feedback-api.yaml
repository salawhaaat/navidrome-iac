{{/* Feedback API is deployed separately via kubectl apply (k8s-feedback-api.yaml + ConfigMap).
     Not managed by this Helm chart to avoid ConfigMap size issues with inline code.
     See: navidrome_mlops/data/docker/k8s-feedback-api.yaml */}}
